@model IEnumerable<Final1_Colegio.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Full_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DNI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Promedy)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Full_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DNI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Promedy)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdStudent }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdStudent }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdStudent })
        </td>
    </tr>
}

</table>
